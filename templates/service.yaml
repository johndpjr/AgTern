apiVersion: v1
kind: Service
metadata:
  name: {{ include "AgTern.fullname" . }}
  labels:
    {{- include "AgTern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AgTern.selectorLabels" . | nindent 4 }}
