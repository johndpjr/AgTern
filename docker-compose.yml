version: "3.8"

services:
  frontend:
    build:
      context: frontend
      dockerfile: ../docker/frontend/Dockerfile
    ports:
      - "4200:80"
#  backend:
#    build:
#      context: ./
#      dockerfile: docker/backend/Dockerfile
#    ports:
#      - "8080:8080"
